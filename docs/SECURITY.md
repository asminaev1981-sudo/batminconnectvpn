# Security

- токены хранятся только через Android Keystore/flutter_secure_storage;
- конфигурации не выводятся в обычные логи;
- API работает только по HTTPS;
- debug-сборка не предназначена для публикации;
- release-ключ не хранится в репозитории;
- телеметрия выключена по умолчанию.
